import { IsEmailTakenDirective } from './is-email-taken-directive';

describe('IsEmailTakenDirective', () => {
  it('should create an instance', () => {
    const directive = new IsEmailTakenDirective();
    expect(directive).toBeTruthy();
  });
});
