import { RoleBaseView } from './role-base-view';

describe('RoleBaseView', () => {
  it('should create an instance', () => {
    const directive = new RoleBaseView();
    expect(directive).toBeTruthy();
  });
});
