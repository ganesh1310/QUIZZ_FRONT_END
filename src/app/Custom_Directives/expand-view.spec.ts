import { ExpandView } from './expand-view';

describe('ExpandView', () => {
  it('should create an instance', () => {
    const directive = new ExpandView();
    expect(directive).toBeTruthy();
  });
});
